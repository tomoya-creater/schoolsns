import 'package:flutter/material.dart';

class FunHub extends StatelessWidget {
  const FunHub({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text("FunHub"),
      ),

    );
  }
}
